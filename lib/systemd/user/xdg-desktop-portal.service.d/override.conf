[Unit]
# Hyprland never activates graphical-session.target, so Requisite would
# prevent the portal from starting. Drop to After-only like the Hyprland
# backend unit does. The vlarch-graphical-session.service handles pulling
# in graphical-session.target so other services that need it still work.
Requisite=
