---
description: Bump version.txt when shipping Vlarch changes to main
alwaysApply: true
---

# Vlarch release version

When you finish a user-facing change, bump the repo root `version.txt` (patch increment, e.g. `0.0.111` → `0.0.112`) in the same commit, then commit and push to `main` unless the user explicitly says not to push or to skip versioning.
